@model MovieStore.Models.Domain.Movie
@{
}
<div class="movie-detail w-80 m-auto">
    <div class="img-container">
        <img alt="image" src="/Upload/@Model.MovieImage">
    </div>

    <div class="detail-container">
        <h3>Title: @Model.Title</h3>
        <h4>Genre Names: @Model.GenreNames</h4>
        <h4>Release Year: @Model.ReleaseYear</h4>
        <h4>Cast : @Model.Cast</h4>
        <h4>Director : @Model.Director</h4>
    </div>
</div>
<div class="w-80 m-auto">
    <a href="/Home/Index" class="btn btn-default">Back</a>
</div>